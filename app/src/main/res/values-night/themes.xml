<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Assistencias" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/light_pink</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icon_attend</item>
        <item name="windowSplashScreenAnimationDuration">1300</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Assistencias</item>
    </style>
</resources>